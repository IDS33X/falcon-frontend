import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
    searchBar: {
        maxWidth: 500,
        borderRadius: 50,
        marginLeft: '-20px',
        marginBottom: '3px'
    }
}));
export default useStyles;